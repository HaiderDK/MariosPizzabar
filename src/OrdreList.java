import java.util.ArrayList;
import java.util.List;

public class OrdreList {
    /*private List<Ordre> orders; // Liste til at holde alle bestillinger

    // Konstruktør
    public OrdreList() {
        orders = new ArrayList<>(); // Initialiserer listen
    }

    // Tilføjer en ordre til liste
    public void addOrder(Ordre ordre) {
        orders.add(ordre);
    }

    // Vist alle bestillinger i rækkefølge
    public void showOrders() {
        if (orders.isEmpty()) {
            System.out.println("Ingen bestillinger er blevet foretaget.");
        } else {
            for (Ordre ordre : orders) {
                ordre.finalizeOrder(); // Vist ordrebekræftelse for hver ordre
                System.out.println();
            }
        }
    }*/
}

